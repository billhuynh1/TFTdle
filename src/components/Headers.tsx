import Streak from "./Streak.tsx";

const Headers = () => {
  // Make StreakCounter component, PatchNotes component, and HowToPlay component
  return (
    <div className="headers">
      <Streak /> | Patch notes | how to play
    </div>
  );
};

export default Headers;
